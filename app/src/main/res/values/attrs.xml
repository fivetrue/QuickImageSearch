<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->


    <declare-styleable name="ImageLayoutSet">
        <attr name="title" format="string" />
        <attr name="icon" format="reference" />
    </declare-styleable>

    <declare-styleable name="ImageSelectionViewer">
        <attr name="sendAction" format="boolean" />
        <attr name="actionButtonBackground" format="reference" />
        <attr name="actionButtonText" format="string" />
        <attr name="cancelButtonText" format="string" />
    </declare-styleable>
</resources>
